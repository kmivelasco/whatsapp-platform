# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/whatsapp_mvp

# JWT
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRES_IN=7d

# WhatsApp credentials are now stored per-bot in the database
# Configure them through the Settings > Bot Config UI

# OpenAI
OPENAI_API_KEY=your-openai-api-key
OPENAI_DEFAULT_MODEL=gpt-4o

# Server
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Client
VITE_API_URL=http://localhost:3001/api
VITE_WS_URL=http://localhost:3001
